# Copy this file to .env and fill real values locally.
# Never commit .env. In CI, set these under GitHub → Settings → Secrets and variables → Actions.

# ── Plaid ───────────────────────────────────────────────────────────────────────
PLAID_CLIENT_ID=<set-in-github-secrets-or-local>
PLAID_SECRET=<set-in-github-secrets-or-local>
PLAID_ENV=production
# JSON map or k=v; pairs separated by semicolons
# e.g., {"chase":"access-...","bofa":"access-..."}  OR  chase=access-...;bofa=access-...
PLAID_ACCESS_TOKENS=<set-in-github-secrets-or-local>

# ── Azure OpenAI ───────────────────────────────────────────────────────────────
AZURE_OPENAI_ENDPOINT=https://<resource>.openai.azure.com
AZURE_OPENAI_API_KEY=<set-in-github-secrets-or-local>
AZURE_OPENAI_DEPLOYMENT=gpt-4o-mini
AZURE_OPENAI_EMBEDDINGS=text-embedding-3-large
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# ── Paths ──────────────────────────────────────────────────────────────────────
OUTPUT_DIR=data/raw   # this path is gitignored

# ── Feature flags (safe-by-default) ────────────────────────────────────────────
EMAIL_ENABLED=0
EMAIL_DRY_RUN=1
PDF_ENABLED=0

# ── Windows-only (optional) ───────────────────────────────────────────────────
WKHTMLTOPDF_PATH=C:\Program Files\wkhtmltopdf\bin\wkhtmltopdf.exe
